[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tfg-onitama"
version = "0.1.0"
description = "Onitama engine + CLI"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["onitama", "cli", "ai"]
